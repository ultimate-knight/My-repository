let admin;         // Declare a variable named 'admin' without assigning a value
let vain;          // Declare a variable named 'name' without assigning a value

vain = "john";     // Assign the string "john" to the variable 'name'
admin = vain; // Assign the value of 'name' to the variable 'admin'
alert(vain)
alert(admin)
