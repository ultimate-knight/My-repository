let message = "hello";
message = 123456;
SpeechRecognitionAlternative(message); // Use console.log to print messages in Node.js

alert(1/0);               // Division by zero will result in 'Infinity'.
alert(Infinity);
alert("not a number" / 2); // This will result in 'NaN'.
alert(NaN + 1);           // This will also result in 'NaN'.
alert(3 * NaN);           // This will result in 'NaN'.
alert("not a number" / 2 - 1); // Handle this error as needed.
