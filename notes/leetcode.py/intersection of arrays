def intersection(self,nums1,nums2):
    nums1=[1,2,2,1]
    nums2=[2,2]
    set1=set(nums1)
    result=[]
    print(nums2)
    for num in nums2:
        if num in set1:
            result.append(num)
    
    return set(result)